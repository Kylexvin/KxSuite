import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  experimental: {
    // If you're using the app directory (which you are)
  },
  // Add path aliases if needed, but Next.js handles @/* by default
};

export default nextConfig;